{
  "name": "hwglu-ctf-2-web",
  "version": "1.0.0",
  "private": true,
  "description": "HWG-LU IT-Sicherheit WS 2025/26 — CTF Challenge 2: Web-Attacks (client-side). Static site + Cloudflare Pages Function for per-student flag personalization.",
  "scripts": {
    "build": "echo 'no build step — static files in public/, functions in functions/'",
    "dev": "npx wrangler pages dev public --compatibility-date=2024-11-01",
    "deploy": "npx wrangler pages deploy public"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {}
}
